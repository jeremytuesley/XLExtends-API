const { getAllProducts } = require('./getAllProducts');
const { getProduct } = require('./getProduct');
const { login } = require('./login');
const { signRequest } = require('./signRequest');

module.exports = { getAllProducts, getProduct, login, signRequest };
